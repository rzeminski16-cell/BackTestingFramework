# Full runtime environment for the BackTestingFramework (GUIs + dashboard).
#
# Dependency metadata lives in pyproject.toml (single source of truth, with
# extras: gui / dashboard / shap / dev). This file is the convenience path
# for a complete setup:  pip install -r requirements.txt
# For reproducible installs use requirements.lock instead.

# Core
pandas>=1.5.0
numpy>=1.21.0

# Excel report generation
openpyxl>=3.0.9

# Visualisations
matplotlib>=3.5.0
scipy>=1.7.0
seaborn>=0.11.0

# Desktop GUIs (needs system tkinter, e.g. `sudo apt-get install python3-tk`)
customtkinter>=5.2.0

# Alpha Vantage API client
requests>=2.28.0

# Columnar storage for the data-preparation run package (Parquet export)
pyarrow>=12.0.0

# Optimization
scikit-optimize>=0.9.0
pyyaml>=6.0

# Modelling & Evaluation stage (ML diagnostics on the prepared run package)
scikit-learn>=1.3.0
statsmodels>=0.14.0
# shap is optional: enables SHAP summaries for ensemble finalists. The tool
# falls back to permutation importance + coefficients/tree rules if it is absent.
# shap>=0.43.0
joblib>=1.2.0

# Interactive results dashboard (apps/modelling_dashboard.py)
streamlit>=1.30.0
plotly>=5.18.0
