# Core dependencies for BackTestingFramework
pandas>=1.3.0
numpy>=1.21.0

# Excel report generation
openpyxl>=3.0.9

# Enhanced visualizations (optional but recommended for advanced reports)
matplotlib>=3.5.0
scipy>=1.7.0
seaborn>=0.11.0  # Optional: for enhanced heatmaps

# Optional: For GUI
# tkinter  # Usually included with Python, but listed for reference

# Optimization
scikit-optimize>=0.9.0
pyyaml>=6.0

# Performance optimization (optional but recommended)
# Provides 5-20x speedup for indicator calculations via JIT compilation
numba>=0.56.0
